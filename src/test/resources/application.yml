server:
  port: 7900

spring:
  security:
    unionid:
      enabled: true
      authc:
        platform-parameter: platform
        unionid-parameter: unionid
        token-parameter: token
        path-pattern: /login/unionid
        post-only: true
